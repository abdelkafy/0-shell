use std::env;
use crate::models;

pub fn run(cmd : models::Ls) {
  let current_path= env::current_dir().unwrap();
  let entries = std::fs::read_dir(&current_path).unwrap();
  if !(cmd.all){
    entries.filter(|&entrie| entrie)
  }
  if cmd.long{

  }
  if  cmd.classify {
      
  }
}

struct LongFormat {
  file_type:String,
  permissions:String,
  hard_links_pointing:String,
  owner_name:String,
  group_name:String,
  modification_time:String,
  file_name:String,
}